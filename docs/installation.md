# Installation

```json
{
  "mcpServers": {
    "git-flow": {
      "command": "npx",
      "args": [
        "-y",
        "git-flow-mcp@latest"
      ]
    }
  }
}
```

### Prerequisites

- Node.js 20 or later
